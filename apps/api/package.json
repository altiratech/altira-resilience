{
  "name": "@resilience/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'build handled by wrangler'",
    "db:migrate:local": "wrangler d1 migrations apply altira-resilience-db --local",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured yet'",
    "test": "vitest run"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "hono": "^4.6.10",
    "unpdf": "^1.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240229.0",
    "typescript": "^5.6.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.28.1"
  }
}
